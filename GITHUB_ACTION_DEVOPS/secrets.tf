# JFrog credentials are stored in HashiCorp Vault (secrets/creds/jfrog).
# SonarCloud token is stored as a GitHub Actions repository secret (SONAR_TOKEN).
# AWS Secrets Manager is no longer used by this pipeline.
