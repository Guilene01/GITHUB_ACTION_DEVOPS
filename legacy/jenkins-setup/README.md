# jenkins-setup (Legacy)

This folder contains a legacy Jenkins deployment example. It is retained for historical reference only and is not part of the current active repository architecture.

For the new workflow, use `GITHUB_ACTION_DEVOPS/` and `FULL_INFRA_WITH_DOCKER/`.
