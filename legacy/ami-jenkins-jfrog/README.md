# ami-jenkins-jfrog (Legacy)

This folder contains a legacy Jenkins + JFrog AMI deployment and is retained for reference only.

The current repository focus is on `GITHUB_ACTION_DEVOPS/` and `FULL_INFRA_WITH_DOCKER/` using GitHub Actions, JFrog, Vault, and SonarCloud.
